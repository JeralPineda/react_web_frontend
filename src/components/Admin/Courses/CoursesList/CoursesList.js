import React from 'react';

const CoursesList = ({ courses, setReloadCourses }) => {
   console.log(courses);

   return (
      <div>
         <h1>Courses List...</h1>
      </div>
   );
};

export default CoursesList;
